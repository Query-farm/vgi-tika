# name: test/sql/zz_probe_map.test
# group: [vgi_tika_probe]
require-env VGI_TIKA_WORKER

statement ok
LOAD vgi;

statement ok
ATTACH 'tika' AS tika (TYPE vgi, LOCATION '${VGI_TIKA_WORKER}');

# Probe B: table function returning the meta MAP column.
query I
SELECT meta['Content-Type'] FROM tika.extract('test/sql/data/hello.pdf');
----
application/pdf
