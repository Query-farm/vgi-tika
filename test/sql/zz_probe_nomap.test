# name: test/sql/zz_probe_nomap.test
# group: [vgi_tika_probe]
require-env VGI_TIKA_WORKER

statement ok
LOAD vgi;

statement ok
ATTACH 'tika' AS tika (TYPE vgi, LOCATION '${VGI_TIKA_WORKER}');

# Probe A: table function returning NO map column (mime/n_pages/content only).
query I
SELECT mime FROM tika.extract('test/sql/data/hello.pdf');
----
application/pdf
